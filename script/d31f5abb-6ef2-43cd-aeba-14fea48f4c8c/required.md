R语言绘制相关性热图, 包括显著性检验结果, 例如p值和q值, 使用 *表示显著性水平, 例如 * 表示 p < 0.05, ** 表示 p < 0.01, *** 表示 p < 0.001. 用户可以选择是否显示这些显著性标记在图表上。

我的目标是, 首先根据json生成的form表单生成一个交互式的界面，用户可以选择一个文件然后选择一些列作为 x 轴的变量，选择另外一个文件然后选择一些列作为 y轴的变量, 计算相关性及其显著性检验结果, 然后根据用户的选择图形参数,包括相关性颜色, 生成相关性热图